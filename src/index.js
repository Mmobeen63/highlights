import React from "react";
import ReactDOM from "react-dom/client";
import HighlightText from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <HighlightText />
  </React.StrictMode>
);
